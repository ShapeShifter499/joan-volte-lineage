# joan-volte-lineage

VoLTE for the LG V30 (`joan` — US998 / H930 / H932) on **LineageOS 22**,
packaged as a flashable recovery zip. No Magisk, no stock `Ims6` blobs, no
CAF `OpenIMSd`, no `libvssims-impl.so`.

It ports a VoLTE path that was first **proven on postmarketOS** on this
handset (REGISTER 200, INVITE 180/200 with PCMU audio, SMS both ways) onto a
stock-shaped Android ROM. The longer-term intent is to offer this upstream so
LineageOS can ship working VoLTE for joan rather than requiring a side-load.

> **Status: bring-up, not a release.** SIP REGISTER currently gets a 401 from
> the P-CSCF that is not yet root-caused. See [Current state](#current-state).

## How it works

Two pieces, because Android splits the problem in two:

| Component | Lives in | Role |
|---|---|---|
| `joan-ims-ua` | `native/` (C, static aarch64) | The SIP/AKA/IPsec user agent. Builds REGISTER, runs RFC 3310 digest AKA, installs the IPsec SAs the P-CSCF demands. |
| `org.joan.ims` | `ims-service/` (Java) | An Android `ImsService`. Talks to the SIM for AKA, and drives the daemon. |

They are split because neither half can do the other's job:

- The Java service **cannot program IPsec**. Writing xfrm from an app domain
  is blocked by a platform `neverallow`, so the native helper is not an
  optimisation — it is the only way.
- The native daemon **cannot talk to the SIM**. AKA authentication goes
  through the telephony framework, which is Java-side.

So the app performs the ISIM AKA exchange and hands the daemon the resulting
`RES`/`CK`/`IK`, and the daemon does the networking.

The daemon runs from `/system/bin/joan-ims-ua` in SELinux domain `netmgrd`,
because that domain already holds the kernel xfrm permissions this needs.
It lives on `/system` rather than `/vendor` because joan's dynamic-partition
layout leaves the vendor logical partition with no free space.

### Control channel

The app reaches the daemon over the abstract unix socket `@joan_ims_ctl`,
one request line to one response line (`REG1`, `REG2`, `NET`, `ID`,
`STATUS`, …).

The daemon authenticates every peer with `SO_PEERCRED` and refuses any uid
not on its allowlist — uid 0, plus whatever
`/data/vendor/netmgr/joan-ims.allowuid` lists (one uid per line). This
matters because `REG2` carries AKA key material.

An earlier revision also listened on `127.0.0.1:15090`. That has been
removed: any app holding `INTERNET` could reach it, and authenticating a TCP
peer requires reading `/proc/net/tcp`, which returns `EACCES` from the
`netmgrd` domain (measured on-device). A control channel that injects key
material and cannot identify its caller is not worth keeping.

## Building

```sh
make -C native test      # host unit tests, checked against the pmOS reference
make -C native device    # aarch64 daemon -> native/build/joan-ims-aarch64
./scripts/pack-zip.sh    # full pipeline -> out/joan-volte-recovery.zip
```

Build dependencies: `aarch64-linux-gnu-gcc`, a JDK (`javac`, `keytool`,
`jarsigner`) and `python3`.

`python3` is **build-time only**, for APK signing and zip assembly inside
`scripts/pack-zip.sh`. Nothing Python ships to the device and nothing Python
runs on it — the installed artifacts are the C daemon and the APK. (An
earlier design did ship a Python UA; that is gone. Replacing the remaining
`python3` heredocs with `zip`/`apksigner` would drop the dependency
entirely, and is worth doing before any upstream submission.)

The host unit tests are checked against digest vectors generated by the
postmarketOS Python implementation that actually achieved REGISTER 200, so
they encode a known-good oracle rather than the C code's own opinion.

## Installing

Flash in **Lineage recovery** (TWRP cannot apply this ROM's dynamic
partitions):

1. LineageOS 22 zip (sideload)
2. GApps, if wanted (sideload)
3. `out/joan-volte-recovery.zip` (sideload; skip signature check)

The installer is a files-only `update-binary`: it places the daemon, the
init `.rc`, the APK and its priv-app permission XML, and labels the binary
`netmgrd_exec`. It does **not** modify sepolicy — an earlier revision
appended CIL, which turned out never to be in the loaded policy on this
device, so that was dropped.

`scripts/pack-cleanup-zip.sh` builds a companion zip that removes a previous
install.

Do not flash `abl` / `xbl` / `tz` / `hyp` / `keymaster` / `laf` from stock.

## Debugging

The daemon logs to `/data/vendor/netmgr/joan-ims-ua.log` — chosen because
it is the one directory both SELinux (`netmgrd_data_file`) and the DAC bits
(`radio:radio` 0770) permit for this daemon.

The first line of every run is a positive-control banner naming the sink:

```
[27304.487] joan-ims: [I] --- joan-ims-ua start: log sink open (/data/vendor/netmgr/joan-ims-ua.log) ---
```

**If that line is absent, the log channel is dead and nothing else in the
file is evidence of anything.** This is not hypothetical: a previous
revision opened a log path whose parent directory did not exist and
discarded the error, and the daemon ran for hours producing no output while
looking perfectly healthy. `STATUS` therefore also reports the sink state
(`LOG=<path>` or `LOG=none errno=N`), so a dead log is diagnosable without
needing the log you cannot read.

## Current state

Working:

- IMS PDN comes up; `ImsService` binds; the daemon runs Enforcing as `netmgrd`
- ISIM AKA exchange returns `sw=9000`; RES/CK/IK reach the daemon
- IPsec SAs install (ESP transport, auth-trunc)
- REGISTER reaches the P-CSCF and is challenged

Not working:

- **REG2 gets a 401.** Root cause not yet isolated.
- MT (inbound) calling is unproven; MO calling and dialer wiring are not wired up.

Known defects, in the order they are worth attacking:

1. `cnonce` and `nc` are hardcoded (`"cnonce01"` / `00000001`), so every REG2
   retry replays an identical pair. A replay-detecting P-CSCF would 401 every
   attempt after the first — the leading suspect for a 401 that survives
   daemon restarts.
2. `integrity-protected=yes` is asserted unconditionally, even when
   `xfrm_install()` only partially succeeded, in which case REG2 goes out
   unprotected while claiming otherwise.
3. **AKAv2-MD5 is advertised but never computed.** The challenge algorithm is
   echoed into the `Authorization` header, but the digest always uses
   `password = RES`; `CK`/`IK` are discarded. A host probe confirms an AKAv1
   and an AKAv2 challenge produce an identical response hash. Not the current
   401 (this carrier challenges AKAv1) but a hard blocker for other carriers.
4. A quoted `algorithm="…"` in the challenge is silently discarded and falls
   back to AKAv1-MD5.
5. `hex_decode()` returns a partial count on a malformed nibble, so a 32-char
   RES corrupted at byte 8 is accepted as a valid 64-bit RES.

## Carrier support

The goal is carrier-neutral. The daemon has no compiled-in realm: it takes
the realm from the SIM (ISIM domain / IMPI suffix) and the P-CSCF address
from PCO, both supplied over the ctl channel.

It has so far only been *exercised* against one carrier, and defects 3 and 4
above are the known gaps between "no hardcoded carrier" and "actually works
on another carrier".

## Credits

Ported from a postmarketOS VoLTE bring-up on the same handset. Kernel-side
work (mainline joan) lives in a separate tree.

## License

Not yet declared — to be settled before upstream submission.
