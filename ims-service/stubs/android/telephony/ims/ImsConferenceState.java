package android.telephony.ims;

import android.util.ArrayMap;

/** Compile-time stub for AOSP ImsConferenceState (not in public SDK). */
public class ImsConferenceState {
    public static final String PARTICIPANT_USER = "user";
    public ArrayMap<String, String> mParticipants = new ArrayMap<>();
    public ImsConferenceState() {}
}
