package android.util;

import java.util.HashMap;

/** Compile-time stub of the framework ArrayMap class (not in public SDK). */
public class ArrayMap<K, V> extends HashMap<K, V> {
    public ArrayMap() {}
    public ArrayMap(int capacity) {}
}
