<?xml version="1.0" encoding="utf-8"?>
<!-- Privapp permissions for the Joan ImsService priv-app. -->
<permissions>
    <privapp-permissions package="org.joan.ims">
        <permission name="android.permission.MODIFY_PHONE_STATE" />
        <permission name="android.permission.READ_PRIVILEGED_PHONE_STATE" />
        <permission name="android.permission.READ_PRECISE_PHONE_STATE" />
        <permission name="android.permission.CONNECTIVITY_USE_RESTRICTED_NETWORKS" />
        <!-- BIND_IMS_SERVICE removed with the uses-permission that
             needed it: the service guards itself with android:permission
             and the app never held it for any purpose. AOSP's
             privapp-permissions_com.android.imsstack.xml does not list
             it either. -->
        <!-- Load-bearing: the manifest requests LOCATION_BYPASS, and a
             requested signature|privileged permission with no entry here
             is a fatal boot error under ro.control_privapp_permissions=
             enforce, not a silent denial. AOSP's ImsStack allowlists the
             same one. -->
        <permission name="android.permission.LOCATION_BYPASS" />
        <permission name="android.permission.RECORD_AUDIO" />
        <permission name="android.permission.MODIFY_AUDIO_SETTINGS" />
    </privapp-permissions>
</permissions>
